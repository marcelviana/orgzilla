/**
 * DEPRECATED: Este arquivo foi movido para @/lib/types
 *
 * Use a nova estrutura:
 * ```tsx
 * import type { Pessoa, Time, Database } from '@/lib/types'
 * ```
 *
 * Este arquivo está aqui apenas para compatibilidade retroativa.
 * Será removido em versões futuras.
 */

// Re-export tudo de @/lib/types para compatibilidade
export * from '@/lib/types'
